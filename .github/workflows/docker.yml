name: Java CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Install Maven
        run: |
          sudo yum install maven -y

      - name: Build with Maven
        run: mvn clean install -DskipTests

      - name: Login to Docker Hub
        run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}

      - name: Build Docker image
        run: docker build -t rawatdocker145/examify:latest .

      - name: Publish image to Docker Hub
        run: docker push rawatdocker145/examify:latest

  deploy:
    needs: build
    runs-on: [self-hosted]

    steps:
      - name: Pull Image from Docker Hub
        run: docker pull rawatdocker145/examify:latest

      - name: Delete old container if exists
        run: docker rm -f examify-container || true

      - name: Run Docker container
        run: docker run -d -p 8080:8080 --name examify-container rawatdocker145/examify:latest
